// * this is a User Profile nextjs typescript page

import { NextPage } from "next";
import UserProfilePage from "../../../components/User/UserProfilePage";

const Profile: NextPage = () => {
    return <UserProfilePage />;
};

export default Profile;
